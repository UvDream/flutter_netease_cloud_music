import 'package:flutter/material.dart';

class CenterBlockPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(
        children: <Widget>[_block(0xe601, '本地音乐', 0)],
      ),
    );
  }

  Widget _block(icon, title, num) {
    return Container(
      child: Row(
        children: <Widget>[],
      ),
    );
  }
}
